#include <stdio.h>

int main(void) {
    int age = 14;
    int year;
    int yaer;

    printf("year: %d\n", year);
    yaer = age + 2000;

    printf("age: %d\n", age);
    printf("yaer: %d\n", yaer);

    age += 1;
    printf("age: %d, yaer: %d\n", age, yaer);
}